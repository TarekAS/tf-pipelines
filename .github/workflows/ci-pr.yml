name: ci

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build.yml

  deploy:
    needs: [build]
    name: Deploy to pr-${{ github.event.pull_request.number }}
    uses: ./.github/workflows/deploy.yml
    with:
      environment: dev
      suffix: pr-${{ github.event.pull_request.number }}
